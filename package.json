{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Personal Portfolio",
  "scripts": {
    "build": "tsc",
    "vercel-build": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "live-server": "^1.2.2"
  }
}